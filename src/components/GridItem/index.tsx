// exporta o proprio arquivo do grid, que é o GridItem.tsx, exporta tudo do GridItem, le o arquivo do componente principal e exportar ele.
// Nunca mais precisamos mexer nesse arquivo agora.
// isso serve para ter o arquivo GridItem.tsx em vez do arquivo index.tsx para cada componente em pasta, já que já temos um arquivo na raiz do projeto chamado index.tsx.
export * from './GridItem';